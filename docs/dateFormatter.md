# date Formatter

```swift
// a date formatter let you decide on the way a date is represented.

import foundation

static var dateFormtter: DateFormatter = {
    let df = dateFormatter()
    fd.dateFormat = "dd/MM/yy"
    return df
}() 

```
